"""Smoke test automatisé API + frontend — 32 scénarios de validation.

STATUT : stub — voir backlog Phase 0.18.
Le CI échouera tant que ce script n'est pas implémenté et vert.

Scénarios prévus :
1. Smoke test (page, statut, chat basique)
2. Chat & Conversations (envoi, historique, CRUD)
3. Agents (profils, assignation modèle)
4. Settings (backend, offline, file auth)
5. Cyber & Skills (workflows, toggles)
6. Vision (upload image)
7. Moniteurs (statut, responsive, raccourcis)
"""
import pytest


class TestSmokeFrontendAPI:
    """32 scénarios de validation frontend + API."""

    def test_placeholder(self):
        """TODO: implémenter les 32 scénarios (voir backlog 0.18)."""
        raise NotImplementedError("Smoke tests pas encore implémentés — voir backlog Phase 0.18")
